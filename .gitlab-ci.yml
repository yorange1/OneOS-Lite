variables:
  GIT_SUBMODULE_STRATEGY: recursive
  GIT_STRATEGY: fetch

stages:
  - check
  - build

check:format:
  stage: check
  tags:
    - win_check
  script:
    - python ci/format_check/run-clang-format.py --clang-format-executable ci/format_check/clang-format.exe -r sdk/

build:windows:keil:
  stage: build
  tags: 
    - win_keil
  script:
    - ci/build/build_keil.bat
